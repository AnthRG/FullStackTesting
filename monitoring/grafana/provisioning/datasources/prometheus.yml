apiVersion: 1
datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      # Salta de un pico en una grafica a la traza concreta que lo provoco.
      exemplarTraceIdDestinations:
        - name: trace_id
          datasourceUid: tempo
